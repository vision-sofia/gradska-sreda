<div class="container">
    <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="{{ path('app.homepage') }}"><img src="{{ asset('images/logo.png', 'front') }}"></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app.homepage') }}">Начало</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app.map') }}">Карта</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app.info') }}">Информация</a>
                </li>
                {% if is_granted('ROLE_MANAGE') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('manage.index') }}">Контролеп панел</a>
                    </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app.user.settings.profile') }}">Настройки</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Изход</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app.register') }}">Регистрация</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app.login') }}">Вход</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</div>
