{% extends 'front/info/layout.html.twig' %}

{% block content %}
    <h2>Информация</h2>
    <p>Описание и цел на платформата</p>

    <h2>Статистика</h2>
    <ul>
        {% for user in users %}
            <li><a href="{{ path('app.users.profile', {'id': user.id}) }}">{{ user.username }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
